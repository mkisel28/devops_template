# Development Environment Configuration
# Registry settings
REGISTRY_PORT=5000
REGISTRY_UI_PORT=8080
TRIVY_PORT=8081

# Nginx settings (not used in dev)
NGINX_HTTP_PORT=80
NGINX_HTTPS_PORT=443

# Registry configuration
REGISTRY_DOMAIN=localhost
REGISTRY_USERNAME=admin
REGISTRY_PASSWORD=admin123

# SSL/TLS (self-signed for dev)
SSL_ENABLED=false

# Logging
LOG_LEVEL=debug

# Environment
ENVIRONMENT=development
